<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/icomoon (1)/style.css">
    <link rel="icon" type="image/png" href="/threads.png"> 
    <title><%= user.username %></title>
</head>
<body>
    <div class="min-h-screen bg-[#0a0a0a] text-white font-sans">
      <div class="max-w-2xl mx-auto py-10 px-4">
        <!-- Profile Header -->
        <div class="flex flex-col items-center gap-4">
          <img src="<%= user.avatar || '/profile.png' %>" alt="avatar" class="w-28 h-28 rounded-full object-cover border-4 border-[#222] shadow-lg">
          <h1 class="text-2xl font-bold mt-2"><%= user.username %></h1>
          <p class="text-gray-400 text-center max-w-md"><%= user.bio || 'لا يوجد نبذة بعد.' %></p>
        </div>
    
        <!-- Divider -->
        <div class="border-b border-gray-800 my-8"></div>
    
        <!-- User Posts Grid -->
        <div>
          <h2 class="text-xl font-bold mb-4">المنشورات</h2>
          <% if (posts && posts.length > 0) { %>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
              <% posts.forEach(post => { %>
                <div class="bg-[#181818] rounded-xl p-4 shadow flex flex-col gap-2">
                  <div class="flex items-center gap-2 mb-2">
                    <img src="<%= post.author.avatar || '' %>" class="w-8 h-8 rounded-full object-cover">
                    <span class="font-semibold text-sm"><%= post.author.username %></span>
                    <span class="text-xs text-gray-500 ms-auto"><%= moment(post.createdAt).fromNow() %></span>
                  </div>
                  <div class="text-gray-200 text-base leading-relaxed">
                    <%= post.content %>
                  </div>
                  <div class="flex gap-4 mt-2 text-gray-400 text-sm">
                    <span class="flex items-center gap-1"><i class="icon-heart-outline"></i> <%= post.likes.length %></span>
                    <span class="flex items-center gap-1"><i class="icon-bubble"></i> <%= post.comments.length %></span>
                  </div>
                </div>
              <% }) %>
            </div>
          <% } else { %>
            <div class="text-center text-gray-500 py-10">لا توجد منشورات بعد.</div>
          <% } %>
        </div>
      </div>
    </div>
</body>
</html>
